<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.covid.web.mapper.MemberDao">
 	<select id="getMemberByEmail" resultType="com.covid.web.dto.Member">
        SELECT id, `name`, password, email, create_date, modify_date FROM member WHERE email = 'carami@example.com';
    </select>
</mapper>
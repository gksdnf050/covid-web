<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.covid.web.mapper.MemberRoleDao">
	<select id="getRolesByEmail"
		resultType="com.covid.web.dto.MemberRole">
		SELECT mr.id, mr.member_id, mr.role_name
		FROM member_role mr JOIN `member` m ON mr.member_id = m.id
		WHERE m.email = #{email}
	</select>
</mapper>